# Architecture Overview

This project uses a modular C++20 structure.

- `src/print_hello_world.cpp` defines an exported module.
- `src/main.cpp` imports that module and executes the application entry point.
